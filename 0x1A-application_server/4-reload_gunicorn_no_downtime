#!/usr/bin/env bash
# This script gracefully reloads gunicorn

PIDS=$(pgrep -f "gunicorn")

if [ -z "$PIDS" ]; then
  echo "No gunicorn pid found."
else
  for PID in $PIDS; do
    kill -HUP "$PID"
  done
fi
